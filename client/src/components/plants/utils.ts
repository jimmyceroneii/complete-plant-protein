import { EssentialProtein } from "./types";

export const removeProteinsWithPlant = (
  plantToRemove: string,
  initialProteinList: EssentialProtein[],
): EssentialProtein[] => {
  // find proteins for current plant

  // determine if they are unique to plant

  // remove if so
  return initialProteinList;
};
